<%= form_with(model: breed, local: true) do |form| %>
  <% if breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(breed.errors.count, "error") %> prohibited this breed from being saved:</h2>

      <ul>
      <% breed.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :breed_name %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :breed_image %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :breed_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
